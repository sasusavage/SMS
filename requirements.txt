Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-Login==0.6.3
Flask-Bcrypt==1.0.1
Flask-WTF==1.2.1
WTForms==3.1.2
SQLAlchemy==2.0.36
alembic==1.15.2
Flask-Limiter==3.12
psycopg2-binary==2.9.11
python-dotenv==1.0.0
gunicorn==23.0.0
# Optional: server-side PDF for report cards. Needs native libs (pango/cairo/
# gdk-pixbuf) — see nixpacks.toml. If those libs are missing at runtime the app
# still works; PDF degrades to browser Print → Save as PDF.
weasyprint==62.3
